# SetGripper.srv - /piper/set_gripper
# Gripper control service

# Request
float32 position               # mm (0=closed, max=open)
int32 speed                    # 1-1000 (default 500)
bool wait                      # wait for completion
---
# Response
bool success
float32 actual_position        # mm
string message
