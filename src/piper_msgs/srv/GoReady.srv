# GoReady.srv - /piper/go_ready
# Move to ready position

# Request
int32 speed                    # 1-100% (default 30)
bool open_gripper              # open gripper (default false, close is safer)
---
# Response
bool success
string message
float32[] position             # [x,y,z,r,p,y] mm/degrees
